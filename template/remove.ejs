<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <title>Remove</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            color: #333;
        }

        header {
            background-color: red;
            color: white;
            text-align: center;
            padding: 1rem 0;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        h1 {
            margin: 0;
            font-size: 2rem;
        }

        main {
            max-width: 600px;
            margin: 2rem auto;
            padding: 2rem;
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        form {
            margin-top: 1rem;
        }

        button {
            padding: 0.5rem 1rem;
            background-color: #e53935;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1rem;
        }

        button:hover {
            background-color: #d32f2f;
        }

        a {
            display: inline-block;
            margin-top: 1rem;
            text-decoration: none;
            color: red;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }

        footer {
            text-align: center;
            padding: 1rem 0;
            background: red;
            color: white;
            margin-top: 2rem;
        }
    </style>
</head>

<body>
    <header>
        <h1>Removing All Courses</h1>
    </header>

    <main>
        <form action="/remove-classes" method="POST" onsubmit="return confirmRemove()">
            <button type="submit">Remove all courses</button>
        </form>
        <a href="/">&#8592; Back to Home</a>
    </main>

    <footer>
    <div id="api-result">
      <p>Random Facts: <%= facts %></p> 
    </div>
    </footer>

    <script>
        function confirmRemove() {
            const confirmation = window.confirm("Are you sure you want to remove all courses?");
            return confirmation;
        }
    </script>
</body>

</html>
